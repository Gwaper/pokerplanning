# Environment variables for Docker Compose
# Copy this file to .env for local docker deployment

# Backend
PORT=3001
NODE_ENV=production

# Database
DB_HOST=postgres
DB_PORT=5432
DB_NAME=mydb
DB_USER=postgres
DB_PASSWORD=postgres

# Frontend
VITE_API_URL=http://localhost:3001
